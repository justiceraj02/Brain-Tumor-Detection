services:
  - type: web
    name: brain-tumor-detection
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python setup_model.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
